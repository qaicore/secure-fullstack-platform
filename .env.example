# Generate a 64-byte random string and paste it here
# PowerShell: [Convert]::ToBase64String((1..64 | %{ Get-Random -Max 256 }))
JWT_SECRET=replace_me

# Optional: how long tokens last. Default: 1h
# Examples: '15m', '1h', '7d'
JWT_EXPIRES_IN=1h